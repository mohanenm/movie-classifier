
text <- ("file for changing text to vector for train, validation, test")
print(noquote(text))


if(FALSE) {

    doesnt R, store text as vectors, so scikit the data is already how it should be?
    or is this wrong
}

# scikit learn library for python?

